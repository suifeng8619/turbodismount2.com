---
import '../styles/global.css';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description: string;
  ogImage?: string;
  canonicalUrl?: string;
  type?: 'website' | 'article';
  publishedTime?: string;
  modifiedTime?: string;
  isHome?: boolean;
  noIndex?: boolean;
}

const {
  title,
  description,
  ogImage = '/og-default.png',
  canonicalUrl,
  type = 'website',
  publishedTime,
  modifiedTime,
  isHome = false,
  noIndex = false,
} = Astro.props;

const siteUrl = 'https://turbodismount2.com';
const fullTitle = title.includes('Turbo Dismount 2') ? title : `${title} | Turbo Dismount 2`;
const canonical = canonicalUrl || new URL(Astro.url.pathname, siteUrl).href;
const ogImageFull = ogImage.startsWith('http') ? ogImage : `${siteUrl}${ogImage}`;
---

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{fullTitle}</title>
  <meta name="description" content={description} />
  <meta name="robots" content={noIndex ? "noindex, nofollow" : "index, follow"} />
  <link rel="canonical" href={canonical} />
  <link rel="alternate" hreflang="en" href={canonical} />
  <link rel="alternate" hreflang="x-default" href={canonical} />

  <!-- Preconnect for third-party domains -->
  <link rel="preconnect" href="https://www.googletagmanager.com" />
  <link rel="preconnect" href="https://store.steampowered.com" />
  <link rel="preconnect" href="https://steamcommunity.com" />
  <link rel="preconnect" href="https://www.crazygames.com" />
  <link rel="preconnect" href="https://shared.st.dl.eccdnx.com" />
  <link rel="preconnect" href="https://cdn.fastly.steamstatic.com" />
  <link rel="preconnect" href="https://cdn.cloudflare.steamstatic.com" />
  <link rel="preconnect" href="https://img.youtube.com" />


  <!-- Open Graph -->
  <meta property="og:title" content={fullTitle} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={ogImageFull} />
  <meta property="og:url" content={canonical} />
  <meta property="og:type" content={type} />
  <meta property="og:site_name" content="Turbo Dismount 2 Wiki & Guides" />
  {publishedTime && <meta property="article:published_time" content={publishedTime} />}
  {modifiedTime && <meta property="article:modified_time" content={modifiedTime} />}

  <!-- Twitter Card -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={fullTitle} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={ogImageFull} />

  <!-- Theme color -->
  <meta name="theme-color" content="#0D0D0F" />

  <!-- Favicon -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="manifest" href="/manifest.json" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />

  <!-- RSS -->
  <link rel="alternate" type="application/rss+xml" title="Turbo Dismount 2 Updates" href="/feed.xml" />

  <!-- Schema.org - WebSite (homepage only) -->
  {isHome && <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "Turbo Dismount 2 Wiki & Guides",
    "url": siteUrl,
    "description": "Complete wiki, guides, vehicle database, and workshop resources for Turbo Dismount 2"
  })} />}

  <!-- Schema.org - Organization (homepage only) -->
  {isHome && <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "turbodismount2.com",
    "url": "https://turbodismount2.com",
    "description": "Unofficial fan site providing guides, databases, and resources for Turbo Dismount 2"
  })} />}
</head>
<body class="min-h-screen flex flex-col">
  <Header />
  <main class="flex-1">
    <slot />
  </main>
  <Footer />

  <!-- Google Analytics -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-CVNT0YNE9E"></script>
  <script is:inline>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-CVNT0YNE9E');
  </script>
</body>
</html>
